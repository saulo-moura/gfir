<?php
if ($acao=='alt')
{
    /*
    $sqlt  = 'select po.* ';
    $sqlt .= '  from parametro_obra po ';
    $sqlt .= ' where ';
    $sqlt .= '   po.idt_empreendimento = '.null($_GET['idt0']);
    $rst = execsql($sqlt);
    if ($rst->rows == 0) {
        // obra sem parâmetros de default
    }
    ForEach ($rst->data as $rowt) {
        $idt                        = $rowt['idt'];
        $data_financiamento         = $rowt['data_financiamento'];
        $percentual_desconto        = $rowt['percentual_desconto'];
        $percentual_financiamento   = $rowt['percentual_financiamento'];
        $taxa_anual                 = $rowt['taxa_anual'];
    }
    */


/*

    $dtaw1 = date('d/m/Y H:i:s');
    $dtaw = trata_data($dtaw1);

    
    $row['nome_completo_login']  = $nome_solicitante;
    $row['email_completo_login'] = $email_solicitante;
    
    $tira=Array();
    $tira = explode('@',$email_solicitante);
    
    $row['usuario_login']        = $tira[0];
    $row['perfil_padrao_login']  = 'INICIAL';

    
    $idt_sistema_ambiente = $row['idt_sistema_ambiente'];
    $idt_usuario          = $row['idt_usuario'];
    $idt_cargo            = $row['idt_cargo'];
    $nome_solicitante     = $row['nome_solicitante'];
    $email_solicitante    = $row['email_solicitante'];
    $idt_gestor           = $row['idt_gestor'];
    $permite_login        = $row['permite_login'];
    $permite_sistema      = $row['permite_sistema'];
    $status               = $row['status'];
    $fecha_gestor         = $row['fecha_gestor'];
    $fecha_administrador  = $row['fecha_administrador'];
    $idt_administrador    = $row['idt_administrador'];
*/
    



}
?>